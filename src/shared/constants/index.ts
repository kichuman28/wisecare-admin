/** Route paths — single source of truth for navigation */
export const ROUTES = {
    LOGIN: '/login',
    DASHBOARD: '/',
    SERVICE_REQUESTS: '/service-requests',
    USERS: '/users',
    ALERTS: '/alerts',
} as const;
